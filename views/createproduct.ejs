<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/createproduct.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="flashmessages">
        <% if (messages.error && messages.error.length > 0) { %>
            <div class="emsg alert alert-danger">
              <%= messages.error[0] %>
            </div>
          <% } %>
          
          <% if (messages.success && messages.success.length > 0) { %>
            <div class="smsg alert alert-success">
              <%= messages.success[0] %>
            </div>
          <% } %>
    </div>   
    <div class="page">
        <div class="menu">
          <h1><span style="color: rgb(57, 179, 227); font-weight: 600;"> Scratch </span></h1>
            <h4>All Products</h4>
            <h4>Create New Product</h4>

        </div>
        <div class="main">
            <div class="nav">
            </div>
            <form action="/products/create" method="post" enctype="multipart/form-data">
            <h3>Create New Product </h3>
            <h4 class="mainh4">Product Details </h4>
            <h5>Product Image</h5>
            <input type="file" name="image" id="image" placeholder="Select File" >
            <div class="input1">
                <input type="text" name="name" id="name" placeholder="Product Name">
                <input type="text" name="price" id="price" placeholder="Product Price">
            </div>
            <br><br>
            <h4>Panel Details</h4>
            <div class="input2">
            <input type="text" name="bgcolor" id="bgcolor" placeholder="Background Color">
            <input type="text" name="textcolor" id="textcolor" placeholder="Text Color">
            <input type="text" name="panelcolor" id="panelcolor" placeholder="Panel Color">
            </div>
            
            <button type="submit" class="btn btn-primary">Create Product</button>

            </form>
            
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"></script>
</body>
</html>